
import chalk from 'chalk';

export async function packageCommand(directory: string, options: any): Promise<void> {
    console.log(chalk.cyan('Packaging Web+ application...'));
    console.log(chalk.gray('Package command will be implemented in v0.2'));
}
